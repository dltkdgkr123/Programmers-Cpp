#include <string>
#include <iostream>
using namespace std;

bool solution(string s)
{
    int cnt = 0;

    for (char c : s)
    {
        if (c == 'p' || c == 'P')
            cnt++;
        else if (c == 'y' || c == 'Y')
            cnt--;
    }

    return !cnt; // 반환값 바뀌어야하므로 !
}
